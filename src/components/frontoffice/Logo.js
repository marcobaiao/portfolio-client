function Logo({ className }) {
  return (
    <h1
      className={`w-full font-bold text-white font-heading text-[2.2rem] ${className}`}
    >
      Marco Baião
    </h1>
  );
}

export default Logo;
